# wordwork 真机验收缺陷修复任务

## 角色和目标

你正在 `D:\tools\wordwork` 仓库中继续开发 `wordwork`。请以当前仓库代码为唯一代码基线，完成本文件列出的修复、自动测试、Windows 构建与部署自检，并在仓库根目录新增一份 `CLAUDE_MANUAL_FIX_DELIVERY.md` 作为交付报告。

不要只修改文档或绕过错误；必须修复根因。不要删除或重置 `C:\ProgramData\wordwork` 的数据库、对象文件、证书、日志或用户数据。不要重建测试项目来掩盖旧数据兼容问题。不要复制无明确许可证的第三方代码。

## 最终产品规则（以此为准）

- 老师和学生使用 Tauri Windows EXE，不要求用浏览器操作。
- 学生可以在尚未提交时查看其他成员已经提交的说明、差异、高风险片段并发表评论。这是产品允许的行为，**不是权限漏洞，不要限制或删除**。
- 只有老师能接受/拒绝片段、完成审阅、跳过提交、解决冲突、恢复版本和发布结果；这些服务端 403 边界必须保持。
- 公式、图片、域、图表、超链接等复杂对象不要求在差异界面内部完整渲染，也不做内部字符级合并；必须标记为高风险并明确告诉老师预览/红线可能不完整，需要人工核验，且绝不能静默丢失对象。
- 已发布轮次的 `base_version_id` 必须永久冻结；恢复当前主版本不能改变旧轮次基础版本。
- 所有版本仍是不可变快照，恢复仅切换当前版本指针并写审计事件即可，不强制复制一份新对象。

## 真机验收中已经通过的功能（修复时不得回归）

1. 老师登录、建项目、添加学生、上传初始 DOCX、发布第 1 轮。
2. 新学生首次登录会进入无法跳过的强制改密页；改密后旧密码失效、新密码登录成功且不再弹窗。
3. A、B 两名学生都从同一个冻结基础版本 `#19` 下载/编辑/提交；B 下载的仍是老师底稿，没有混入 A 的修改。
4. 普通文字差异能够显示真实删除/插入及正确作者；单条接受、全部接受和撤销决定可用。
5. 含公式的修改被标记为两个高风险片段；不显示公式内部结构本身符合产品取舍。
6. 未完成审阅时发布按钮禁用；服务端也应继续拒绝遗漏非终态提交。
7. `/submissions/{submission_id}/skip` 部署正确后可用，跳过状态能显示。
8. 历史列表包含主版本、成员提交和红线稿；恢复学生提交、再恢复老师 `#19` 均成功，历史没有丢失。
9. 恢复当前主版本以后，第 1 轮基础版本仍为 `#19`，冻结语义正确。
10. 学生 C 未提交即可查看 A/B 差异和发表评论，这是用户确认接受的正常功能。

## 必须修复的问题

### P0-1：下载到一个新文件名必然误报“Word/WPS 占用”

真机复现：无 WINWORD/WPS/LibreOffice 进程时，下载工作副本或历史版本到尚不存在的新路径，按钮长期显示下载中，随后提示“文件正被 Word/WPS 占用”，没有创建文件。预先在目标位置创建一个同名 DOCX，再选择覆盖则成功。

已定位：`apps/desktop/src-tauri/src/main.rs` 的 `is_file_released` 用 `OpenOptions::new().write(true).open(path)`；目标不存在时返回 `NotFound`，却被当成 `false`。调用点包括 `RoundPage.tsx` 和 `VersionsPanel.tsx` 的保存流程。

修复要求：

- 目标不存在应立即视为可写，但仍要正确处理父目录不存在、无权限、路径是目录等真正错误。
- 已存在且被 Word/WPS 独占的文件仍不能覆盖。
- 最好不要把所有 I/O 错误压成一个布尔值；返回可区分的状态/错误，界面不要把“权限不足”“目录不存在”误报为 Word 占用。
- 工作副本下载、历史版本下载、红线下载、人工合并相关保存入口使用同一可靠逻辑。
- 取消保存对话框后按钮应立即恢复；超时或错误后 `busy` 必须复位。
- 增加 Rust 单元测试或可测试的纯函数，至少覆盖：不存在目标、普通已存在文件、目录路径、只读/权限错误；客户端测试覆盖取消和错误后的 busy 复位。

### P0-2：本地工作副本和离线队列没有按账号隔离

真机复现：学生 A 退出后登录学生 B，B 页面仍显示 A 的“我的工作副本”；后来学生 C 登录，页面直接显示 `student-b_round1.docx`，并提供“用 Word 打开/提交”。点击“忘记这份工作副本”才会清除。这会导致共享电脑泄露本地路径、打开他人文档，甚至以错误账号提交。

已定位：`apps/desktop/src/lib/store.tsx` 的 `PersistedState` 只有全局 `workingCopies` / `queue`；`upsertWorkingCopy` 和 `removeWorkingCopy` 只按 `roundId + baseVersionId` 匹配，没有 server/account/member 维度。请同时检查草稿监测、自动补传、登出/登录和服务器地址切换路径。

修复要求：

- 工作副本与离线队列至少按规范化服务器地址 + 稳定 `member.id` 隔离；用户名可显示但不能作为唯一安全键。
- 当前会话只能读取、打开、监测、删除、提交自己的条目；切换账号或服务器不得出现上一身份的数据。
- 不要简单在登出时永久删除所有条目：离线队列和工作副本可能需要用户稍后以原账号恢复。可以迁移为 per-account buckets，并只隐藏非当前 bucket。
- 旧 `STATE_VERSION=1` 数据必须安全迁移：无法确定所有者的遗留工作副本/队列不得自动归给新登录用户。应隔离为 legacy/orphaned，给出“旧数据需要重新关联或清除”的安全提示，或采取更保守的不可提交处理。
- 快照文件也应按账号命名空间组织或用元数据强校验；自动补传前再次验证队列 owner 与当前 token/member 一致。
- 补充测试：A 建立副本/离线项→退出→B 登录看不到且不能提交；A 再登录仍可恢复自己的条目；切换服务器隔离；旧状态迁移不误归属；C 不会看到 `studentB_round1.docx`。

### P0-3：部署组件版本不一致导致“完成审阅”稳定返回 500

真机日志：

```text
File "D:\tools\wordwork\services\api\app\main.py", line 1054, in finalize_review
    if applied.needs_manual_review:
AttributeError: 'WindowsPath' object has no attribute 'needs_manual_review'
```

当前源码 `packages/doc_engine/src/wordwork_doc_engine/engine.py` 的 `apply_decisions` 返回带 `needs_manual_review` 的结果对象，当前 `services/api/app/main.py` 依赖该契约；但 `C:\ProgramData\wordwork\venv` 实际加载的旧 `wordwork_doc_engine 0.1.0` 返回 `WindowsPath`。结果是任何提交都无法完成审阅，`完成审阅 0/2`，发布本轮结果始终不可用。

修复要求：

- 先确认仓库版本号、wheel/egg metadata、`build/lib` 陈旧副本、Windows 安装脚本以及服务实际 import 路径，找出旧引擎为何仍被加载。不要只在 `finalize_review` 用 `hasattr` 吞掉契约错误。
- 安装/升级脚本必须强制把当前仓库的 doc engine 安装/升级到服务 venv；避免 pip 因同版本号或缓存保留旧代码。可采用一致递增版本、`--upgrade --force-reinstall --no-deps`（依赖另行可靠安装）、可编辑安装或其他可证明可靠的方案。
- 清理或避免打包/导入 `packages/doc_engine/build/lib`、`services/api/build/lib` 等陈旧构建产物；明确服务究竟从哪里 import。
- `/healthz` 不能只返回 `engine_available: true`。增加不会触碰正文的契约/版本自检字段，例如 API 版本、引擎版本、契约版本，以及 `apply_decisions` 返回模型能力标识；不兼容时服务应启动失败或健康检查明确不健康，不能等老师点“完成审阅”才 500。
- Windows 安装脚本部署结束后必须用 **服务实际使用的 Python** 输出 `wordwork_doc_engine.__file__`、包版本和契约版本，并做一个最小引擎 smoke test；任何不匹配直接让安装失败。
- API 对意外引擎契约错误应记录可诊断错误码并返回友好 5xx，但这只是兜底，不能代替版本一致性。
- 增加集成测试：用部署方式安装到干净临时 venv，启动 API，上传小 DOCX、提交、决策、finalize，确认得到 reviewed/contribution；同时构造旧契约替身，确认启动自检或健康检查能阻止使用。

### P1-1：复杂对象风险提示不够明确

真机中公式段落被正确标为“高风险”，但差异页没有明显说明“公式不会在此完整显示、红线稿可能不完整、必须下载原提交/红线并人工核验”。用户一度将公式不显示理解为软件损坏。

当前 API 已有 `redline_warnings`，`RoundPage.tsx` 也有 warning 映射逻辑。请检查为什么部署界面没有显示承诺的醒目横幅：可能仍是前后端部署版本、告警生成条件或 UI 展示位置问题。

要求在提交列表和差异窗口顶部显示人类可读的醒目提示，并明确区分：

- “界面不展示公式内部差异”（设计限制）；
- “红线稿未能完整表达某些变更”（实际告警）；
- “该片段必须人工合并，系统不会自动套用”（处理要求）。

不得为了消除提示而把复杂对象转成纯文本，也不得静默删除图片、公式、域、超链接或字符格式。

### P1-2：历史红线审阅稿可直接恢复为当前主版本，容易误操作

版本历史页面对 `redline` 也显示“恢复”。用户没有实际点击这一项，但从界面可以确认入口存在。请根据业务语义收紧：普通主版本、发布结果、人工合并结果、成员原始提交可以恢复；`redline` / `summary_redline` / 草稿等审阅或中间产物默认不得直接设为正式主版本。若确需保留高级能力，必须二次警告并由服务端同时验证，不能只隐藏按钮。

补充前端与 API 权限/类型测试，已知 version id 也不能绕过类型限制。

### P2：接受或“全部接受”时页面会闪一下

功能结果正确，但真机每次接受操作都会明显闪烁。检查是否每个决定都把整个 diff 置为 loading/卸载重建，尽量局部更新决定状态，失败时再回滚；不要牺牲服务端真实状态一致性。添加组件级测试，保证操作时弹窗不卸载、滚动位置和当前筛选不丢失。

## 不要误修的事项

- 不要禁止学生 C 在提交前查看或评论 A/B 差异；用户已明确认为这是正常功能。请相应修正文档中“提交后才可查看”的旧描述，使实现、README 和界面文案一致。
- 不要尝试在 HTML 中完整渲染公式，也不要自动合并公式内部字符差异。
- “恢复”当前实现直接移动 `Document.current_version_id` 并写审计，真机已验证能来回恢复且旧轮次仍冻结；不要改成覆盖或删除历史对象。
- 不要把早期的空白差异页和 skip 404 当作当前代码缺陷：那是 Windows 服务当时没有重启到新版。服务真正更新后差异页与 skip 均正常。
- 首次改密已经真机通过，不要重新放开“稍后再说”。

## 验证要求

先阅读 `plan.md`、`CLAUDE_DELIVERY.md`、`CLAUDE_FIX_DELIVERY.md`、`README.md`、`docs/ARCHITECTURE.md`、`docs/SECURITY.md`、`docs/DEPLOY-WINDOWS.md`，再修改代码。保留用户已有修改，禁止 `git reset --hard`、禁止删除正式数据。

至少执行并在交付报告中记录完整结果：

```powershell
pnpm test
pnpm typecheck
.\.venv\Scripts\python.exe -m pytest services\api\tests packages\doc_engine\tests -p no:cacheprovider -q
cargo test --manifest-path apps\desktop\src-tauri\Cargo.toml
```

此外必须增加并运行以下端到端回归：

1. 新路径下载成功，文件 SHA-256 与服务端版本一致；已打开文件被拦截；取消保存不假死。
2. A/B/C 三账号在同一 Windows 用户下轮流登录，工作副本与离线队列严格隔离。
3. 从干净 Windows/临时目录按实际安装脚本部署，确认服务进程加载当前引擎路径和契约。
4. 老师完成一份普通文字提交审阅后状态变为 reviewed，发布按钮条件正确；含复杂对象提交走明确的人工处理/跳过流程，不返回 500、不丢对象。
5. 历史恢复后既有轮次 `base_version_id` 不变。
6. 学生仍能查看并评论已提交内容，但调用老师专属端点继续得到 403。

若当前机器允许，请构建新的 Windows EXE/安装包，但**不要自动覆盖用户正在使用的 EXE，不要自动重启生产服务**。把待部署产物的完整路径、SHA-256、版本号和管理员部署步骤写入交付报告，由用户决定何时安装。

## 交付报告格式

在 `CLAUDE_MANUAL_FIX_DELIVERY.md` 中写清：

1. 每个问题的根因、修改文件与关键行号。
2. 数据/状态迁移策略，特别是旧的全局工作副本和离线队列如何安全处理。
3. API—doc engine 契约版本如何定义、安装后如何证明加载的是当前版本。
4. 新增测试名称、覆盖场景、实际通过数量和命令输出摘要。
5. 新构建产物路径、SHA-256、版本号、部署与回滚步骤。
6. 仍未完成或必须人工真机验证的事项，不得宣称未运行的测试已通过。
7. 给下一位审查者一份 10 分钟内可执行的最小真机回归清单。

完成后不要只回复“已修复”；请引用交付报告，并简要列出仍需用户批准或手工执行的动作。
