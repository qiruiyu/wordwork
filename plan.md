wordwork`：面向课题组的 DOCX 协同审稿与版本管理软件

## 一、产品目标

构建一个轻量、私有部署、跨 Windows/macOS 的桌面伴侣软件。成员仍使用熟悉的 Microsoft Word 或 WPS 编辑，`wordwork` 负责文档分发、版本备份、差异审阅、修改合并和通知。

首版完成以下闭环：

1. 老师上传主文档并“发布新一轮”。
2. 两名学生收到系统通知，各自下载基于同一版本的工作副本。
3. 成员继续在 Word/WPS 中编辑，不再用文字颜色区分作者。
4. 软件自动备份草稿；成员点击“提交”后生成永久版本。
5. 所有人都能只读查看其他人已经提交的修改和说明。
6. 老师按修改片段接受或拒绝，并处理冲突。
7. 老师发布新的主版本；任何历史版本均可下载和恢复。

不在首版实现：实时多人共同编辑、完整即时聊天、微信机器人、内置 Word 编辑器、`.docm` 宏文档和公式内部的字符级合并。

## 二、开源方案结论

截至 2026-09-13，没有发现一个“小而完整”的项目能够直接覆盖跨平台 DOCX 分发、分支、审阅和合并。建议新建轻量产品，复用成熟的 DOCX 引擎，而不是复制大型办公套件。

| 项目 | 可借鉴内容 | 与本需求的差距 | 采用策略 |
|---|---|---|---|
| [Python-Redlines](https://github.com/JSv4/Python-Redlines) | 跨平台比较两个 DOCX，输出 Word 原生修订；MIT | 不提供用户、项目、版本、推送及多分支合并 | 作为核心差异引擎依赖 |
| [docx-revisions](https://github.com/balalofernandez/docx-revisions) | 枚举、接受、拒绝 DOCX 修订；MIT | 主要聚焦文字修订，不是完整协作产品 | 用于首版文字修订处理 |
| [Adeu](https://github.com/dealfluence/adeu) | 格式保真、修订应用、表格和多文档部件处理；MIT | 代码规模较大，偏 AI 文档工作流 | 不整套复制；仅作为技术验证失败时的固定后备引擎 |
| [docx-actions](https://github.com/JSv4/docx-actions) | GitHub PR 风格的 Word 差异预览；MIT | 强绑定 GitHub Actions | 借鉴审阅界面和测试样例 |
| [MsOfficeDiff](https://github.com/Papyrine/MsOfficeDiff) | 调用 Word 原生比较，项目较小；MIT 源码 | 只支持安装了桌面 Word 的 Windows，官方二进制另有使用条款 | 不作为运行时依赖，可用于 Windows 开发验证 |
| [DocGit](https://github.com/CatOn60Hz/docgit) | Word 侧边栏、提交/分支/回滚的产品思路 | 无明确许可证；合并只到文件级，同一文件修改就冲突；Windows/Word 依赖较重 | 只参考思路，禁止复制代码 |

微软 Word 本身已经支持生成带修订的比较文档、接受单条修订以及合并多位审阅者版本：[文档比较](https://learn.microsoft.com/en-us/office/vba/api/word.application.comparedocuments)、[接受修订](https://learn.microsoft.com/en-us/office/vba/api/word.revision.accept)、[合并多个版本](https://support.microsoft.com/en-us/word/combine-document-revisions)。但为了兼容 macOS Word 和 WPS，首版核心逻辑不依赖 Word COM。

项目暂时私有，但所有第三方依赖必须许可证明确；保留 `THIRD_PARTY_NOTICES`、版本号和上游链接。无许可证的代码不得复制。

## 三、系统架构

### 桌面端

采用 Tauri 2 + React + TypeScript：

- Windows 10/11 与 macOS 13+ 使用同一套界面。
- 通过系统默认程序打开工作副本，不开发编辑器或 Office 插件。
- 在应用数据目录维护本地工作区、基础版本编号和文件哈希。
- 监测 Word/WPS 保存动作；文件稳定五分钟后自动建立草稿快照，同一文件最多每十分钟上传一次。
- 离线时允许继续编辑和本地快照；恢复网络后同步。
- 发布、提交、审阅完成等事件显示应用内未读消息和系统通知。
- 不在 Word/WPS 打开文件期间原地改写该文件；合并结果总是生成新文件。

### 私有服务器

采用单体 FastAPI + Python 3.12：

- Docker Compose 部署到一台私有 Linux VPS。
- Caddy 提供 HTTPS 和证书续期。
- SQLite WAL 保存业务数据，适合首批 3–10 人、单实例运行；通过 SQLAlchemy/Alembic 保留未来迁移 PostgreSQL 的能力。
- DOCX 文件使用 SHA-256 内容寻址存储，重复内容只保存一次。
- 数据库任务表加单独 worker 处理比较、预览、合并，不引入 Redis。
- WebSocket 推送事件；断线后按事件序号补拉。
- 每日加密备份数据库和文件对象，保留最近 30 个日备份；永久版本本身不自动删除。
- 日志只记录版本号、任务状态、耗时和错误码，不记录基金正文。

### 数据模型与接口

核心内部 REST/WebSocket 接口，不对公网提供开放 API。核心类型如下：

- `Project`：课题项目。
- `Member`：成员及 `teacher/student` 角色。
- `Document`：基金正文及当前主版本。
- `Round`：一次协作轮次，包含冻结的 `base_version_id`。
- `Version`：不可变文件快照，包含父版本、作者、时间、SHA-256、展示文件名。
- `Submission`：成员基于某一轮基础版本的提交及说明。
- `ReviewHunk`：可独立接受或拒绝的修改片段。
- `ReviewDecision`：老师的接受、拒绝或待处理决定。
- `MergeConflict`：多个提交修改同一结构块时产生的冲突。
- `AuditEvent`：发布、下载、提交、撤回、审阅、恢复等操作记录。

主要接口：

- `POST /auth/login`
- `POST /projects/{id}/rounds`
- `POST /rounds/{id}/publish`
- `POST /rounds/{id}/submissions`
- `GET /submissions/{id}/diff`
- `PATCH /reviews/{id}/hunks/{hunk_id}`
- `POST /reviews/{id}/finalize`
- `POST /rounds/{id}/publish-result`
- `GET /documents/{id}/versions`
- `POST /versions/{id}/restore`
- `WS /events`

所有提交都必须携带 `base_version_id` 和本地 SHA-256；服务器不根据文件名判断版本。

导出文件名统一为：

`项目名_轮次_作者_YYYYMMDD-HHmm_短版本号.docx`

## 四、DOCX 差异和合并规则

### 文档处理

1. 仅接受有效、未加密的 `.docx`，默认上限 100 MB。
2. 拒绝 `.docm`、损坏压缩包、Zip Slip、压缩炸弹和包含宏部件的伪装 DOCX。
3. 不访问文档中的外部链接；只进行标记和风险提示。
4. 以 `w14:paraId`、文档部件、表格行列位置和内容指纹组合定位结构块。
5. 使用 Python-Redlines/Docxodus 生成带原生修订的红线文档。
6. 使用 `docx-revisions` 枚举和执行正文、标题、列表及表格单元格中的文字修订。
7. 预览 HTML 必须经过白名单清理，不能执行文档内脚本或外部资源。

### 修改片段

- 正文按连续插入、删除或替换修订分组。
- 表格以单元格为边界，禁止一个片段跨越多个单元格。
- 页眉、页ners.
- 图片、公式、图表、域和交叉引用以整体对象处理，显示“高风险结构修改”提示，不做公式内部或图片内部差异。
- 未修改的 OOXML 部件保持原样；不得为方便比较而把整份文件转成 Markdown 再生成 DOCX。

### 多人合并

每轮基础版本在发布后冻结，老师如需改稿，也以一个独立提交参与本轮：

1. 每份提交始终与冻结基础版本比较。
2. 老师对提交中的片段逐项接受或拒绝。
3. 系统先生成“基础版本 + 本提交已接受片段”的解析结果。
4. 来自不同成员、位于不同段落或不同表格单元格的结果可自动合并。
5. 两份提交修改同一段落、同一单元格或同一复杂对象时，一律产生冲突；首版不猜测作者意图。
6. 冲突界面并排展示基础内容、当前合并内容和另一成员内容。老师可以选择其中一方，或导出临时 DOCX，在 Word/WPS 中手工整理后重新上传。
7. 发布结果时生成：
   - 干净的最终主文档；
   - 每位成员的红线审阅文档；
   - 本轮决定与冲突记录；
   - 从基础版本到最终版本的汇总红线文档。
8. 发布后该轮只读，新结果成为下一轮基础版本。

学生提交后可以查看其他人的差异和版本说明，但只有老师能接受、拒绝、解决冲突和发布主版本。

## 五、实施阶段

### M0：DOCX 引擎可行性验证，1–2 周

- 收集 20–30 份脱敏测试文档，覆盖中文字体、标题、编号、表格、图片、公式、页眉页脚、脚注、目录、域和批注。
- 验证 Python-Redlines 输出在 Windows Word、macOS Word、Windows/macOS WPS 中均可打开且无“需要修复文档”提示。
- 验证 `docx-revisions` 对文字片段的接受/拒绝和格式保持。
- 实现两人修改不同段落的自动合并，以及修改同一段落的确定性冲突。
- 若 `docx-revisions` 未通过固定样例，则改用 Adeu 完成对应修订应用；不自行发明第二套文本差异算法。
- 复杂对象不能安全处理时必须转为人工冲突，禁止静默丢弃或重建。

进入后续开发的门槛：全部样例可正常打开；未修改内容不发生可见变化；每项修改都有作者和来源；同一输入重复处理得到相同结果。

### M1：版本库与分发闭环，2–3 周

- 完成账号、角色、项目、文档、不可变版本和审计事件。
- 实现上传主文档、发布轮次、成员下载、草稿自动备份、显式提交、历史版本恢复。
- 完成内容哈希去重、文件名生成、WebSocket/断线补拉和系统通知。
- 完成 VPS 一键部署、每日备份和恢复脚本。

### M2：差异审阅与合并，3–4 周

- 建立异步 DOCX 比较任务和任务状态页面。
- 实现行内及左右对照两种差异视图、作者筛选、片段接受/拒绝和批量操作。
- 实现表格单元格差异、复杂对象风险标识、多人结构块合并和冲突界面。
- 生成干净主文档、单人红线和整轮审计包。
- 让其他学生在提交后只读查看彼此修改并发表评论。

### M3：桌面体验与跨平台发布，2–3 周

- 完成 Tauri 本地工作区、Word/WPS 打开、保存监测、离线快照及恢复同步。
- 构建 Windows 安装包和 macOS 应用包。
- 做异常恢复：Word/WPS 占用文件、网络中断、重复提交、过期基础版本、比较任务失败。
- 增加第三方许可证清单、管理员说明和普通成员操作指引。

### M4：课题组试用，2 周

- 用一份脱敏国基金文档完成两轮三人协作。
- 记录比较时间、冲突率、人工合并次数、文档修复提示和漏改反馈。
- 每周执行一次备份恢复演练。
- 只有在连续两轮无文档损坏、无版本丢失后，才把系统用于正式申报材料。

预计投入：一名开发者全职约 9–12 周；博士阶段兼职开发约 14–18 周。

## 六、测试与验收标准

### 自动测试

- DOCX 黄金样例：中文增删改、跨 run 格式、列表、表格、图片、公式、页眉页脚和脚注。
- 修订测试：单条接受、单条拒绝、替换配对、全部接受、全部拒绝和重复执行幂等性。
- 合并测试：不同结构块自动合并；同一结构块稳定产生冲突；发布后结果可再次作为基础版本。
- API 测试：角色越权、过期轮次、重复上传、断点重试、哈希不一致和并发提交。
- 安全测试：恶意压缩路径、超大解压比、宏伪装、外部关系、HTML 注入和未授权下载。
- 备份测试：从空服务器恢复数据库、对象文件、当前版本指针和审计记录。

### 人工兼容矩阵

- Windows Word、macOS Word、Windows WPS、macOS WPS。
- 每个编辑器执行：打开基础文件、编辑、提交、打开红线、查看修订、打开最终文件。
- 最终文件不得出现修复提示；版式、字体、编号、表格宽度、图片、公式和页眉页脚不得发生无关变化。

### 用户验收

- 老师可在两分钟内发布一轮并看见两位学生的提交状态。
- 学生无需改字体颜色即可完成编辑和提交。
- 任意历史提交均可按作者和时间查找并下载。
- 所有接受/拒绝决定都能追溯到成员、轮次、基础版本和原始提交。
- 任何不能安全自动合并的内容都必须显式形成冲突，不能漏改或静默覆盖。

## 七、Sol、Terra、Luna 子 Agent 执行安排

OpenAI 官方文档确认 Codex 可以显式创建子 Agent，并为不同 Agent 分别指定模型和推理强度；Terra 适合日常开发和代码库探索，Luna 适合边界清楚、重复性高的任务：[模型说明](https://learn.chatgpt.com/docs/models)、[子 Agent 说明](https://learn.chatgpt.com/docs/agent-configuration/subagents)。具体可用性仍取决于账号和客户端当前开放情况。

后续进入执行模式时按以下方式分工：

- **Sol 主 Agent**：控制需求、架构、接口契约、DOCX 合并原则、跨模块集成和最终代码审查。
- **Terra 文档引擎 Agent**：完成 M0、OOXML 锚点、红线生成、修订应用和冲突检测，使用 medium/high。
@No response:
- **Terra 产品开发 Agent**：分阶段负责 FastAPI 服务端或 Tauri 客户端，避免同时修改同一目录，使用 medium。
- **Luna 测试 Agent**：生成脱敏样例、补充参数化测试、检查许可证清单、整理操作文档，使用 low/medium。
- 当前环境最多同时运行三个子 Agent 加主 Agent；写操作按目录独占，文档引擎、服务端和桌面端分别拥有独立目录，合并由 Sol 完成。
- 第一批并行工作以只读调研和测试样例为主；接口冻结后再并行实现，避免多 Agent 同时改共享模型和迁移文件。
- 使用 Terra/Luna 能减少 Sol 在机械性工作的消耗，但子 Agent 各自仍会消耗 token，因此只拆分真正独立、边界明确的任务。

## 八、固定假设

- 内部代号沿用 `wordwork`，名称和图标不阻塞开发。
- 首批用户为 3–10 人，一个老师拥有最终发布权。
- 私有 VPS 是唯一权威版本库；成员电脑只保存缓存和工作副本。
- 首版仅支持 `.docx`，最大 100 MB，不支持密码保护和宏。
- 版本说明、评论和通知纳入首版；完整聊天继续使用现有工具。
- 代码暂时私有，但按未来可开源要求管理依赖和许可证。
- 服务端时间统一保存为 UTC，客户端按本地时区显示。
- macOS 正式分发需要可用 Developer ID 与公证条件；没有 Apple 开发者账号时仅作为内部测试包分发。
